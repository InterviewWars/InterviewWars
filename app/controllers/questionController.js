const db = require('../models/database');

const questionController = {
    addQuestion: function (req, res, next) {gi
      console.log('inside addquestion')
      next();
    }

};

module.exports = questionController;
